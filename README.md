# ModelDocLoadingEmbedding
Load BYOM Models and docs into Teradata

pip install -r requirements.txt

modify manConfig.json to include Model you want to load into Teradata and database connection info. clear text as of now. 

